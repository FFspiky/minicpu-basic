# Golden Trace Reference Environment

本目录使用参考 SoC 生成单周期 EXP6 验收所需的 `golden_trace.txt`。参考环境读取 `../func/obj/inst_ram.mif`；如更换镜像文件名，还需同步修改 `src/soc_lite_top.v` 中的 `INST_COE` 定义。

在 Windows PowerShell 中运行：

```powershell
& 'D:\Vivado\Vivado\2019.2\bin\vivado.bat' `
  -mode batch -source 'D:\CPU_DESIGN\cdp_ede_local-master\mycpu_env\gettrace\run_gettrace_sim.tcl'
```

更新测试镜像后必须重新生成 trace，并运行 `soc_bram/run_vivado/run_soc_bram_sim.tcl` 完成 CPU 对拍。提交时应保证 ROM 镜像与 `golden_trace.txt` 来自同一测试配置。
